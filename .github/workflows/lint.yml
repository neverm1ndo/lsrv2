name: Biome
on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  format-and-lint:
    name: Format & lint
    uses: ./.github/workflows/ci-setup.yml
    with:
      node-version: "24"
      commands: |
        # Format check
        npx biome format . --check
        # Lint / static checks
        npx biome check
